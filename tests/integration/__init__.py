# tests/integration/__init__.py

# This file can be left empty or include setup code for integration tests if needed.

# Example of test-specific setup or shared fixtures (optional):

# import pytest
# import os

# # Example of shared fixture that could be used across integration tests
# @pytest.fixture(scope='module')
# def setup_environment():
#     # Example setup code for integration tests
#     os.environ['DATABASE_URL'] = 'your-database-url'
#     yield
#     # Example teardown code if needed
#     del os.environ['DATABASE_URL']
